const ethers = require('hardhat').ethers;

/**
 * @param {Contract} lootBoxController A contract pointed at a LootBoxController
 * @param {Contract} controlledERC721 A contract pointed at an NFT
 * @param {String} tokenId An ID of a particular NFT edition
 * @param {Array} erc20s An array of ERC20 Contract instances
 */
async function fill(lootBoxController, controlledERC721, tokenId, erc20s) {
    
}

module.exports = fill;