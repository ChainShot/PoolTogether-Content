const ethers = require('hardhat').ethers;

/**
 * @param {String} friendAddress A hexadecimal string address
 * @param {Contract} controlledERC721 A contract pointed at an NFT
 */
async function mintNFT(friendAddress, controlledERC721) {
    
}

module.exports = mintNFT;