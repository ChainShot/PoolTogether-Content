const ethers = require('hardhat').ethers;

/**
 * @param {Contract} pool A Contract pointed at a an ERC721ControlledFactory
 */
async function createNFT(erc721ControlledFactory) {
    
}

module.exports = createNFT;